pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Punk Brew"
include(":app")
include(":simplesearchview")
include(":data")
