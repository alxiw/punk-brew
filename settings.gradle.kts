pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Punk Brew"
include(":app")
include(":presentation")
include(":simplesearchview")
include(":domain")
include(":data")
